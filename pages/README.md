# Navigation

The application uses a unified operational shell with sidebar routing so every workspace shares the same live AIS session, map state, filters, and connection status. The available workspaces are implemented in `src/ui/pages.py` and routed by `app.py`.
